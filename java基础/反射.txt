1.类对象：类加载的产物，封装了一个类的所有信息(类名、父类、接口、属性、方法、构造方法)，即每个类加载到内存后都对应一个class对象，每个类有且只有一个class对象

2.类的对象：基于某个类new出来的对象，也称为实例对象

3.获取类对象方法：
	通过调用实例对象的getClass()方法获取该类的类对象
	类名.class
	Class.forName("包名.类名")，优先使用这个方法